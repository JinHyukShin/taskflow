spring:
  datasource:
    url: jdbc:tc:postgresql:17:///stockpulse_test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6370

jwt:
  secret: test-secret-must-be-at-least-32-characters-long-for-hmac
  access-token-expiry-ms: 300000
  refresh-token-expiry-ms: 600000

logging:
  level:
    com.stockpulse: DEBUG
    org.hibernate.SQL: DEBUG
