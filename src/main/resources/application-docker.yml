spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${POSTGRES_DB:stockpulse}
    username: ${DB_USERNAME:stockpulse}
    password: ${DB_PASSWORD:stockpulse}
  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    com.stockpulse: INFO
    org.hibernate.SQL: WARN
